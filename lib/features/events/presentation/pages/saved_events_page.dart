import 'package:flutter/material.dart';

class SavedEvents extends StatelessWidget {
  const SavedEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Saved Events Page'),
    );
  }
}
